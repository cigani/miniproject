import os
import brian2 as b2
import matplotlib.pyplot as plt
import numpy as np
import seaborn


class writePlot(case):
    def casePlots(self,case)
        methodName = 'number' + str(case)
    def writePlot(title, ylabel, xlabel, legend, yval, xval,case)
        if case is None
            case = title
        if case in ('
